<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mibne.scheduledapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.CreateAccountActivity"></activity>
        <activity
            android:name=".Activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Activities.NoticeActivity"
            android:label="@string/title_activity_notice"
            android:parentActivityName=".Activities.MainActivity">
            <intent-filter>
                <action android:name="com.example.mibne.scheduledapp.NoticeActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.RoutineActivity"
            android:label="@string/title_activity_routine"
            android:parentActivityName=".Activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar1">
            <intent-filter>
                <action android:name="com.example.mibne.scheduledapp.RoutineActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.UserInfoActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activities.RegistrationActivity">
            <intent-filter>
                <action android:name="android.intent.action.RegistrationActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.SettingsActivity"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".Activities.MainActivity" />
        <activity
            android:name=".Activities.SingleNoticeActivity"
            android:label="Details"
            android:parentActivityName=".Activities.NoticeActivity" />
        <activity android:name=".Activities.TodayActivity" />
        <activity android:name=".Activities.TomorrowActivity" />
        <activity android:name=".Activities.ThisWeekActivity" />
        <activity android:name=".Activities.AllCoursesActivity" />
        <activity android:name=".Activities.EnrolledCoursesActivity" />
        <activity
            android:name=".Activities.CreateNoticeActivity"
            android:label="Create Notice"
            android:parentActivityName=".Activities.NoticeActivity" />
        <activity android:name=".Activities.EditRoutineActivity" />
        <activity android:name=".Activities.AddRoutineActivity" />
        <activity android:name=".Activities.EditUserAccountActivity">
            <intent-filter>
                <action android:name="android.intent.action.EditUserAccountActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.ResetPasswordActivity" />

        <service android:name=".Services.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
            <!--
     Set custom default icon. This is used when no icon is set for incoming notification messages.
     See README(https://goo.gl/l4GJaQ) for more.
            -->
            <meta-data
                android:name="com.google.firebase.messaging.default_notification_icon"
                android:resource="@drawable/ic_logo" />
            <!--
                 Set color used with incoming notification messages. This is used when no color is set for the incoming
                 notification message. See README(https://goo.gl/6BKBk7) for more.
            -->
            <meta-data
                android:name="com.google.firebase.messaging.default_notification_color"
                android:resource="@color/colorAccent" />
        </service>

        <activity android:name=".Activities.AddUserActivity" />
        <activity
            android:name=".Activities.ManageUsersActivity"
            android:label="Manage User"
            android:parentActivityName=".Activities.MainActivity"
            android:theme="@style/AppTheme.NoActionBar1" />
        <activity android:name=".Activities.EditUserActivity" />
    </application>

</manifest>